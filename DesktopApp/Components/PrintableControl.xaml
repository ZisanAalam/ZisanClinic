<UserControl x:Class="DesktopApp.Components.PrintableControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DesktopApp.Components"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="200" Background="White">
    <Grid>
        <Grid x:Name="PrintableArea">
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Hello there" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
            <StackPanel x:Name="MainStack" Grid.Row="1"/>
        </Grid>
    </Grid>
</UserControl>
